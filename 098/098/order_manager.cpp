#include"order_manager.h"

namespace AbstractManagers {
	/*
	void Order_Manager::SetCondition(abstracts::Order& order, int condition_id) {
		(order).SetCondition(static_cast<abstracts::Condition>(condition_id));
	}
	*/
}